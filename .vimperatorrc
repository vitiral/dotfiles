" Main commands you need to know
" c - buffer mode, this is what lets you move around and copy+paste. 
"   Use / to find something, c to go into buffer mode and v to go into visual mode

" Autocomplete using Firefox Awesomebar subsystem
set complete=l

" Select the longest autocomplete match
set wildmode='list:full'

" Don't beep
set noerrorbells

" Make Firefox run faster by using JIT
set! javascript.options.jit.chrome=true

" Use characters for hints instead of numbers
" set hintchars=0123456789
set hintchars=asdfghklzxcvnqweop


set tabnumbers
set editor=vim
set sh=/usr/bin/zsh
set gui=none,bookmarks
let mapleader=","

" Scroll up and down faster
nnoremap j 5j
nnoremap k 5k

" Make hints larger (125% is the key point)
:highlight Hint font-size:125%;color:white;background-color:red;padding:2px;

inoremap <C-j> <ESC>
inoremap <C-u> ()<left>
inoremap <C-i> []<left>
inoremap <C-m> {}<left>
inoremap <C-h> <left>
inoremap <C-l> <right>

" Figure out what we do when we can't see the tabs... this should be opened in a new window or something?
" :ignorekeys add docs.google.com

" v in normal mode goes straight into visual mode (useful after find)
nnoremap v cv

" exit c+v mode with q
cnoremap q <ESC>
vnoremap q <ESC>

" Reload configuration file into current browser, useful for updating after editing this file.
command! sourcerc :source ~/.vimperatorrc

" Show properties of an object within JavaScript.
javascript <<EOB
function inspect(what) {
    var nodes = [];
    for (var node in what)
        nodes.push(node);
    liberator.echo(nodes.sort().join(', '));
}
EOB

"===[ Mappings ]========================================================

" Press 'F10' to toggle Vimperator
noremap <F10> :vimperatortoggle<CR>

" Press 'cntrl+h' to go to previous tab
noremap <C-h> :tabprevious<CR>
" Press 'cntrl+l' to go to next tab
noremap <C-l> :tabnext<CR>

" Terminal like copy/paste
inoremap <C-S-c> <C-c>
inoremap <C-S-v> <C-v>

" Special copy paste
inoremap <A-y> <C-c>
nnoremap <A-y> <C-c>
cnoremap <A-y> <C-c>
vnoremap <A-y> <C-c>

inoremap <A-p> <C-v>
nnoremap <A-p> <C-v>
cnoremap <A-p> <C-v>
vnoremap <A-p> <C-v>
