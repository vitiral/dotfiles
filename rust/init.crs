#!/usr/bin/env run-cargo-script
//! rust script run with `cargo-script`
//!
//! ```cargo
//! [dependencies]
//! std_prelude = "0.1.*"
//! stdcli = "0.1.*"
//! ```

extern crate std_prelude;
extern crate stdcli;
use std_prelude::*;
use stdcli::prelude::*;

fn main() {
    eprintln!(
        "Running {}",
        Path::new(file!()).file_name().unwrap().to_string_lossy().as_ref()
    );
}

#[test]
fn it_works() {
}
